snapshots:
    - name: dim_hosts 
      relation: ref('hosts') # Reference to the ephemeral model 
      config:
        schema: gold_schema
        database: AIRBNB
        unique_key: HOST_ID
        strategy: timestamp
        updated_at: HOST_CREATED_AT
        dbt_valid_to_current: "to_date('9999-12-31')"