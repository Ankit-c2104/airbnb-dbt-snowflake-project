models:
  - name: metricflow_time_spine
    description: >
      Daily time spine required by the dbt Semantic Layer / MetricFlow for
      time-based aggregations across metrics.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
